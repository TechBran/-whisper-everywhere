<?xml version="1.0" encoding="utf-8"?>
<!--
  The transcript window's resize handle (W3). RED — the app's own red, the literal behind
  BubbleColours.LIVE_DEFAULT — by owner ruling 2026-09-17 on the tablet: "the resizing arrow, we
  need to make that a color where we can actually see it. I say red." White at 70% on a panel
  whose text is white was a handle nobody found. The ImageView keeps its 28dp touch target and
  6dp padding; only the colour and the alpha changed (4.9.1). Pinned by ResizeHandlePinTest.
-->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="24dp"
    android:height="24dp"
    android:viewportWidth="24"
    android:viewportHeight="24">
    <path
        android:fillColor="#FF5252"
        android:pathData="M21,11L21,3l-8,0l3.29,3.29l-10,10L3,13l0,8l8,0l-3.29,-3.29l10,-10L21,11z"/>
</vector>
