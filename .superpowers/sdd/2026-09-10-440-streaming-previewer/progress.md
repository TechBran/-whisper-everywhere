# 4.4.0 — the on-device word-for-word previewer — ledger

Task 1 (P0); Tasks 2-8 on the engine branch. Spec docs/superpowers/specs/2026-09-10-streaming-previewer-design.md. Plan docs/superpowers/plans/2026-09-10-streaming-previewer.md. Amendment docs/superpowers/plans/2026-09-10-streaming-previewer-amendment-pad.md (WINS over the plan where they disagree).
Rulings ASSUMED (spec §0): R1 canary-only + release note; R2 displaced; R3 default-on; R4 Auto=whisper on multilingual; R6 streaming first. R5 identity: the plan says 4.4.0/89, the amendment supersedes it — **4.4.0 = versionCode 90** (89 is spent on the internal track); the identity bump is the controller's commit, no task in this plan touches versionCode/versionName/ReleaseIdentityTest.
Baseline suite re-measured at f18549b (Step 0, from XML): **178 suites / 2,154 tests / 0 failures / 0 errors.** The plan's 177 / 2,150 was measured at 5efc2be; the four-test / one-suite delta is main's own movement between those commits, not this task's.

Branch deviation (controller's instruction, 2026-09-10): Task 1 was implemented on **feat/4.4.0-previewer** in the worktree C:/Users/bastr/.androidbuild/wt-44, NOT on `main` as the plan's Global Constraints say. The commit is a clean fast-forward candidate for `main` — it touches only the three strip files plus this ledger — so the plan's intent (the strip change does not ride the engine branch) is preserved by merging this commit to `main` before Tasks 2-8 branch off it.

=== Task 1: P0 strip prep (feat/4.4.0-previewer) ===
- Base: f18549b. Commit: THIS commit — `feat(strip): the strip rules gain sessionHasLocalPreview — behaviour-neutral prep for the 4.4.0 previewer` (the hash cannot be written into a file the commit contains; it is the first commit after f18549b on this branch).
- Suite after (from XML): 178 suites / 2,162 tests / 0 failures / 0 errors — baseline + 8 (InFlightStripTest 17 -> 23 = +6; InFlightStripWiringPinTest 7 -> 9 = +2). `:app:assembleDebug` BUILD SUCCESSFUL.
- Interfaces Task 7 consumes, all in FloatingBubbleService.kt: `deltaOwnsPreviewStrip(sessionIsLive, sessionHasLocalPreview)`, `inFlightStripLabel(depth, sessionHasLocalPreview)`, `deltaBlankVisibility(sessionHasLocalPreview, currentlyHidden)` (new), `resolvedTextClearsStrip(sessionIsLive, sessionHasLocalPreview, isFinalizing)`, and the field `@Volatile private var sessionHasLocalPreview = false` — declared and reset beside `sessionIsLive = false`, set true NOWHERE (pinned at 0 occurrences of the literal).
- Behaviour neutrality: with `sessionHasLocalPreview` false at every read site, all four rules answer exactly as 4.3.4 did (the truth table InFlightStripTest pins), and `onDelta`'s blank branch takes its HIDDEN row, which is the same bare `View.GONE` write. Every pre-existing strip test is green unchanged.
- Battery (5/5 observed RED under mutation, GREEN after revert): see the task-1 report.

=== Task 8 (AMENDED): certification, the acceptance sheet Z1-Z13 + S1-S3, the four-pack bundle ===
- Base: b1fceb3. Commits: see **the branch's commits, per task** immediately below. Task 8's own are `ae6f6fe` (the acceptance sheet), `f333168` (the close-out) and review round 1's two fix commits, the last of which is THIS commit (a commit's own hash cannot be written into a file it contains).
- **The branch's commits, per task.** Plan Step 4 requires this list in the close-out and the first revision omitted it entirely (review round 1, B2) — the ledger named 2 of the branch's 47 commits, so a merger could not tell what was being fast-forwarded. Reconstructed with `git log --oneline --reverse a0c7bb2..HEAD` and grouped by the scope each subject line already carries. **Two caveats, because this list cannot supply what the ledger never got:** (i) this ledger has entries for Task 1 and Task 8 only — Tasks 2-7 and the two startup rows have no entry, so there is no recorded per-task suite count to attach to these groups and only the boundary run above is certified; (ii) the task numbers on 2b, 6 and 8 come from the commit subjects themselves, while those on 2, 3, 4, 5 and 7 are inferred from content and should be read as workstream names rather than citations (this task may read only its own plan section).
  - **Task 2 — the streaming pack, its two sources, the `preview_en` module (6):** `f8aa31c` the pack's four pinned files, one verification, two sources; `43ca04b` the pack manager goes pack-first and the Play locate is shared, not forked; `9903b3c` `preview_en` rides Play Asset Delivery, untargeted; then round 1's three — `83bb960` (B1, the pack can actually be asked for and a refusal reaches the latch), `6e8f794` (B2, the primary route gates on storage first and a failed copy leaves nothing behind), `bd28700` (B3, the refusal card speaks its own words, never the chooser's).
  - **Task 2b — the voice pack, the amendment's added task (6):** `a8450be` `tts_kokoro` carries the read-aloud archive as-is; `075b4e0` the pack source sits AHEAD of the download, behind the previewer's discriminator; `4956955` the voice row can ask Play — one shell, its own words, one retry route; then `f30999c` (B1, one install of the voice at a time), `8aceecc` (B2, every voice-install surface routes, not just the Settings row), `f8bc165` (nits 2+6, the row stops re-asking Play per tick and names what it is doing).
  - **Task 3 — the recognizer seam and `StreamingPreviewEngine` (7):** `e7a6e12` the seam, the measured constants, the load-time canary; `0c444ff` (B1, the fake records WHERE input closed, so pad-before-finish is pinned); `c146a0f` (nits 1+12, the verdict's own decode count pinned); `38133aa` the 32 ms feed, decode-while-ready, the cumulative partial and the pad/drain/freeze/recreate commit hook; `346ebe1` (B2, a new session is a new verdict — the three-strike count stops carrying); `a169797` (B1, the re-fed tail is the STREAM's audio, so `audio=`/`rtf=`/`firstPartialMs=` stop being fiction on a cap-cut); `7f142b5` (the tail's accounting and the session boundary pinned).
  - **Task 4 — `PreviewTeeEngine` (3):** `a2de730` PCM mirrored to the previewer beside whisper, and the previewer never commits; `74ba0eb` (B1, a freeze for an already-resolved seq is refused, so the strip cannot lead with words whisper skipped); `9798571` (B2, the delivery goes under the lock too).
  - **Task 5 — the language gate and the default-on switch (1):** `1e32c21`.
  - **Task 6 — the row's words and the two surfaces (5):** `74d6449` `StreamingPackCopy` — the row's words keyed by the source the tap will use; `d78c2ec` the same ruling for the voice row (the Play routes say the bytes are the app's own); `ecc2a32` `sourceOf`, the progress lines, the delete row; `1955745` the two surfaces — the Settings rows and the language step; `a65ecd3` (round 1 B1, only the DELIVERED pack is "included with the app" — the amendment's copy ruling, and battery row 4).
  - **Task 7 — the wiring (1):** `5eb9e1f` one wrap site after the language resolves, the resident recognizer, the gate line.
  - **Startup amendment S1 — the post-trim re-arm (5):** `421c75c` the prewarm's own IDLE/ERROR rule, made pure; `b8de674` the trim's missing counterpart, so the next tap is not the cold one; `983df46` the `warmLocalEngine` census moves deliberately; `dd7bd2b` (round 1 B1, the re-arm may not be the process's FIRST `warmLocalEngine()`); `42cc6e8` (B2, the pin bites the deletion of the one decision it pins).
  - **Startup amendment S2 — the ring and the early recorder (11):** `e86e496` the startup ring — the audio between the tap and the engine, and the pace that replays it; `b2a72aa` the recorder opens at the tap, the seam moves above `connect()`, the cue moves onto readiness; `47250b7` the device-audio mic leak the early recorder makes reachable gets its pin; `74d87b1` the NPU cold load stops being an encoder-only spike figure (**this is the do-not-edit file named in the merge instruction below — comments only**); `ee02e93` the replayed clock, executed; `64c5206` a source switch mid-catch-up must not replay the microphone into the device-audio segment; `e5457cb` (round 1 B1, the consent-ask handover is `switchSource`'s sibling, so it gets its flush); `5a71107` (B2, the flush moves behind the old source's stop+join); `6084e1e` (B3, the handover the connect window swallowed is latched, not lost); `00d6ceb` (B4, the projection answer is honoured through the connect window); `b1fceb3` (B4 nits 4+6, two stale-diagnosis fixes in the same seam).
  - **Task 8 — this task (2 at the close-out, plus this review round):** `ae6f6fe` the acceptance sheet Z1-Z13 + S1-S3; `f333168` the close-out; `3699d03` (round 1 B1, the merge instruction stops guaranteeing a scope it does not have); and THIS commit (round 1 B2, the list you are reading).
  - Group sizes 6 + 6 + 7 + 3 + 1 + 5 + 1 + 5 + 11 + 2 = **47**, which is `git rev-list --count a0c7bb2..f333168`; this round's two fix commits are inside the Task 8 group and above that count.
- **Boundary run, from the purged XML: 201 suites / 2,395 tests / 0 failures / 0 errors.** Run twice — once at the start of the task and once after every battery mutation was reverted — with identical numbers, and `:app:testDebugUnitTest` EXECUTED both times (never UP-TO-DATE; the XML directory was deleted before each run). `:app:assembleDebug` BUILD SUCCESSFUL on the reverted tree, with `compileDebugKotlin` actually executing.
- Suite delta: **+23 suites** over the ledger's f18549b baseline of 178. The 24 test files this branch adds contain 23 JUnit suites — `transcription/stream/ScriptedRecognizer.kt` is the shared fake and produces no XML, which the result directory confirms. Tests: 2,395 against that baseline's 2,154 = **+241**, but read that as a floor rather than this branch's own figure: `main` has since absorbed Task 1 and its fix rounds (a0c7bb2 includes the P0 strip prep, its review fixes and the B5 census test), so main's own current count is above 2,154 and was NOT re-measured — doing so needs a gradle run against `main`, and this worktree may not leave `feat/4.4.0-previewer`.
- **The four-pack release bundle: BUILD SUCCESSFUL.** `app-release.aab` = **5,086,892,220 B (5.09 GB / 4.74 GiB)**, signed, at `C:/Users/bastr/.androidbuild/WhisperEverywhere-44/app/outputs/bundle/release/app-release.aab` — NOT deleted. All three payload gates passed on the way: `verifyNpuPacks` ("all 8 pack variants match the census byte counts and both default variants are empty" — no NPU row changed, which is the amendment's own "what must not change"), `verifyPreviewPack` ("all 4 preview pack files match the streaming catalog's byte counts"), `verifyTtsPack` ("the voice pack's 1 file matches TtsModelManager's archive byte count"). `minifyReleaseWithR8` and `lintVitalRelease` both ran green, which is what the plan wanted the bundle for: the new `com.k2fsa` consumer (`SherpaPreviewRecognizer`) survives R8 under the widened keep at `proguard-rules.pro:76-83`.
- Zipfile listing — the four pack modules are all present, with the payloads they were pinned to carry:

  | module | entries | uncompressed | in-bundle |
  |---|---|---|---|
  | `npu_turbo` | 15 | 4,358,663,554 | 3,451,282,737 |
  | `npu_small` | 15 | 1,447,759,162 | 1,175,837,259 |
  | `tts_kokoro` | 4 | 349,907,869 | 349,957,553 |
  | `base` | 308 | 123,666,697 | 37,947,714 |
  | `preview_en` | 7 | 72,655,741 | 59,181,612 |
  | `BUNDLE-METADATA` | 16 | 64,812,306 | 12,581,627 |
  | `META-INF` + `BundleConfig.pb` | 4 | 98,164 | 32,854 |

  `preview_en/assets/preview_en/` carries the four pinned files at exactly 71,083,163 / 1,307,236 / 259,335 / 5,048 B, and `tts_kokoro/assets/tts_kokoro/kokoro-multi-lang-v1_0.tar.bz2` at exactly 349,906,910 B. Both were additionally re-hashed from disk: all four previewer sha256s match `StreamingPackCatalog.EN`'s literals and the archive matches `c5f7e2d2…ea3298` — the gradle gates check byte counts only, so the digests were verified separately. `npu_turbo` and `npu_small` each still carry their four `#group_soc_*` variants plus the empty `#group_other` default.
- Size sanity: the two new packs add 422,563,610 B uncompressed / 409,139,165 B in-bundle, which is the amendment's predicted "~350 MB (voice) + ~73 MB (previewer)" to the byte. The `tts_kokoro` module is marginally LARGER in-bundle than uncompressed because a `.tar.bz2` cannot deflate further.
- **The acceptance sheet went to the CONTROLLER's path, not the plan's:** `docs/superpowers/sdd/2026-09-02-431-guards-tts/acceptance.md:468-687` (the heading through EOF; the file was 466 lines and is now 687), a new section `## Z`, 221 insertions — the section's 220 lines plus the blank at `:467` that separates it from §K — pure addition, CRLF preserved. The plan's own `docs/superpowers/sdd/2026-09-10-440-streaming-previewer/acceptance.md` was NOT created.
- **The sheet's rows are not the plan's greps, and that is the task's one substantive judgement.** The plan wrote Z1-Z11 as `logcat` greps for `stream-gate:` / `stream-timing:` / `stream-open:`. Every one of those is a Kotlin `android.util.Log` call, and `proguard-rules.pro:85-96` strips `android.util.Log.{v,d,i,w,e}` from release — so on the Play-signed track build the owner installs from, not one of them can ever hit. §A-§F of this same sheet already recorded that discovery empirically for 4.3.1. Each row now leads with its EYE criterion and names the grep as debug-build corroboration, and the section carries a per-row table of what survives.
- **Z11 is marked N/A on the track build, and the reason is a gap, not a formatting problem.** Z11 is only a grep of `stream-open: … canary=pass`, and the canary's verdict reaches the user through no surface at all: `StreamingPackCopy.SETTINGS_DISABLED_ON_DEVICE` is defined and pinned but rendered NOWHERE, which `SettingsScreen.kt:1079-1082` states deliberately and assigns to "Task 7 … and its only reader". On release, a device whose canary fails shows an installed row, a switch that is on, and no words, with nothing explaining why. Not fixed here — adding a rendering site is Task 7's scope and would be unreviewed UI in a certification task. Written into the sheet under "Known limitations" and raised to the controller.
- Z12/Z13's criterion is the row's own TITLE, which is what makes them executable on a log-less build: on a Play install the previewer's row reads "Install"/"Get the English preview model" (`StreamingPackCopy.kt:147-153`) and the voice's reads "Install"/"Get the read-aloud voice" (`TtsModelManager.kt:512-517`), and only a non-Play `playCanDeliver == false` install can say "Download". The sheet makes "Download" the one word that fails the setup and says to stop there.
- S1/S2/S3 folded in as the section's "The startup ring" subsection. S3 became sharper than the amendment's wording: the two haptics are now distinguishable by FEEL (20 ms at the tap, `FloatingBubbleService.kt:4736`; 50 ms at readiness, `:4719`), so "the cue does not fire before the app is ready" needs no log. S2 states the ring's real boundary — 6 s capacity (`StartupRing.kt:191`) against the 4,107 ms measured worst case is ~1.9 s of margin, and past 6 s the OLDEST audio is dropped by design.
- **Battery: a 6-row SPOT-CHECK, not the plan's Step 2 full re-run.** 6 rows applied, 6 observed RED, 6 GREEN after revert, every count read from the per-class XML: `LocalPreviewGateTest` (`sessionLanguage == "en"` -> `!= null`) 1/7 RED -> 0; `StartupRingTest` (`CAPACITY_MS` 6_000 -> 5_000) 2/15 -> 0; `StreamingPackCatalogTest` + `PreviewPackLayoutTest` (`tokens.txt` 5_048 -> 5_049) 1/8 and 2/8 -> 0 and 0; `TtsModelManagerTest` (the FromPack subtitle drops "included with the app") 1/20 -> 0; `StreamDiagTest` (`-> preview=` -> `-> arm=`) 1/5 -> 0; `StartupRingWiringPinTest` (`flushStartupRingAtSourceHandover()` deleted from `switchSource`) 1/13 -> 0. Survivors: **0**. The plan's Step 2 asks for every battery row of Tasks 1-7 re-run; the controller's instruction for this task is to read ONLY the Task 8 section and never the whole plan, and the other tasks' mutations are specified only in their own sections. The rows above were therefore derived from the code and chosen to cover one decision per workstream (the language gate, the ring's size, the pack census, the amendment's copy claim, the diag format, the ring's switch-handover flush). Every mutation was applied and reverted with the Edit tool — no `git checkout` — and `git diff` afterwards is the two build-root redirect hunks and nothing else.
- Identity untouched, as instructed: `app/build.gradle.kts:53-54` still reads `versionCode 89` / `versionName "4.3.4"`, and `ReleaseIdentityTest.kt:84-92` still pins 4.3.4 / **89** (the plan's Step 4 text says it pins 88 — that is stale; 89 landed on the track on 2026-09-10). **4.4.0 = versionCode 90** per the amendment, and that bump is the controller's commit. An AAB built from this branch head therefore cannot be uploaded: 89 is spent.
- **Merge instruction.** Fast-forward `feat/4.4.0-previewer` onto `main` only after (i) the controller's identity commit to 4.4.0 / 90, and (ii) the owner marking **Z1, Z2, Z4, Z12, Z13 and S1** PASS on both devices. Z11 cannot be a gate on the track build by construction (see above).
  - **Scope.** 79 files, and **47** commits over `a0c7bb2` as of the close-out commit `f333168` (`git rev-list --count a0c7bb2..HEAD` — every review-fix commit appended after `f333168` raises that number, so run the command rather than trusting this line). `a0c7bb2` IS `main`'s tip (`git rev-list --count a0c7bb2..main` = 0), so the fast-forward is clean. An earlier revision of this bullet said **46**; that was wrong.
  - **One file on the plan's do-not-edit list WAS edited, and it is raised here for the controller's ruling on spec §10 rather than absorbed.** `app/src/main/java/com/whispereverywhere/transcription/NpuWhisperBackend.kt`, named explicitly at `docs/superpowers/plans/2026-09-10-streaming-previewer.md:34`, was changed in `74d87b1` — **+40/-4, comments and KDoc only**: the `~342 MiB, ~525 ms` encoder-only spike estimate at the `load()` KDoc and again at stage (6) replaced by the measured `nativeInit ~2,480 ms` and `load()` end-to-end **4,107 ms**, which is the number S1 and S2 of the acceptance sheet rest on. All four removed lines are comment lines; no declaration, expression or constant changed and no test moved, so the behavioural risk is nil — but an earlier revision of this bullet asserted the list was untouched, and that sentence's only purpose is to let a merger skip re-verifying spec §10, so it may not be false.
  - **The rest of the list is genuinely untouched**, verified by name against all 79 files of `a0c7bb2..HEAD`, 0 hits each: `transcription/LocalWhisperEngine.kt`, `audio/SileroEndpointer.kt`, `model/WhisperModel.kt`, `model/WhisperModelManager.kt`, `service/CommitCadencePolicy.kt`, `transcription/FinalDeliveryPolicy.kt`, `transcription/TranscriptSink.kt`, `transcription/TranscriptionEngine.kt` (which therefore also gains no member, the other half of the plan's line 34), and every file under `transcription/cloud/`, `transcription/live/` and `transcription/batch/` — so every cloud and live engine.
- Worktree environment fixes this task had to make, both first-time-setup gaps rather than code (details in the task report): the eight `npu_*#group_soc_*` payload directories were EMPTY in the worktree, so `verifyNpuPacks` failed the first `bundleRelease` — they are now NTFS junctions onto the main checkout's census-verified payload (gitignored, read-only to the build, and re-verified by the gate itself). And `keystore.properties` had never been copied into the worktree, so the release signing config was empty and `signReleaseBundle` died on a bare `NullPointerException` inside bundletool — copied from the main checkout, never read, never printed, gitignored at `.gitignore:113`.
